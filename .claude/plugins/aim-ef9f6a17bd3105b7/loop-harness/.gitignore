__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
# loop state that never belongs in git (when this repo self-hosts the loop)
.loop/stamp.json
.loop/HALT
.loop/handoff.log
.DS_Store

tickets

*.code-workspace

# Claude Code machine-local state (vendored plugins, global agents/skills)
.claude/

# manifesto tooling per-run hook markers (ephemeral, machine-local)
.manifesto/.hook-seen-*
