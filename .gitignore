tmp
.devcontainer/.env
.venv
.cache
.ssh
.graveyard
.claude/settings.local.json
**/.claude/settings.local.json
**/.claude/scheduled_tasks.lock

# Keep prod.tfvars local — real values go in git-ignored files; ship .example copies.
**/vars/prod.tfvars

# Per-ticket loop worktrees are linked checkouts, not content to track.
.loop/worktrees/

.DS_Store

.claude

# APM dependencies
apm_modules/
