eval: F14-foundation-role-taxonomy

**Definition of Done:** `admin` exists as a policy and an empty group, and is
proven not to be root. The scaffold exists, ships no app-specific groups, and
one throwaway entry is carried end to end: applied, wired to the smoke
assignment, logged in, decoded out of the token, and removed.
`docs/cluster-roles.md` lays out the four roles and `docs/vault-human-auth.md`
no longer gives a conflicting answer.

**A `terraform plan` proves nothing here.** A Vault identity group with no OIDC
assignment gets its member exactly nowhere, and a group that creates cleanly can
still be useless. Rows 4 to 8 are one end-to-end run against a real login.

**A successful login proves nothing either.** A malformed `groups` template
renders `{"groups": "null"}`, passes apply and still issues a valid token. Row
6 decodes.

| Behavior | Input | Expected | Scorer | Threshold |
| --- | --- | --- | --- | --- |
| **`admin` is a working wildcard, not a bare glob** | Get a token that is **entity-backed** — log in as an identity whose entity is a member of `admin`, do not use `vault token create -policy=admin`, which yields `entity_id: ''` and makes the self-entity probe below meaningless. This needs the hand-add from the membership row to have happened first, so run these two in that order. Attempt the things `developer` is denied: `vault read sys/audit`, `vault kv list -mount=bootstrap /`, `vault operator key-status`. Then attempt the paths `default` names: `vault list sys/leases/lookup`, and update the token's **own** entity | All succeed. The last two are the point: `default` shadows `path "*"` on every exact path it names, because a specific path beats a glob, so a policy that is one bare `path "*"` block fails here while passing everything above it | deterministic check (all probes exit 0, including `sys/leases/lookup` and the self-entity update) | 100% |
| **`admin` is not root, and the docs must not say it is** | With the same token: `vault token create -policy=root` | Refused with `root tokens may not be created without parent token being root`. Recorded so nobody builds on `admin` as a root equivalent | deterministic check (non-zero, that message) | 100% |
| **Terraform does not own `admin`'s membership** | `vault read identity/group/name/admin` on a **pristine** group, straight after its first apply and before any hand-write. Then add the **operator** entity to it by hand — not the scratch entity, which rows 6 and 8 require to be in `app-probe-reader` and nothing else — run `terraform plan`, and `terraform apply`. Then force a real change to the group (add and remove a `metadata` entry) and apply again | Before the hand-add, `member_entity_ids` is **`null`, not `[]`** — with `external_member_entity_ids = true` Terraform never sends the field, so Vault has nothing to return. An unflagged group returns `[]`, so a scorer asserting `[]` fails a correct build and passes a broken one. After the hand-add, the plan proposes no change to membership and the member survives **both** applies, including the one that genuinely modifies the group. That second apply is the case that matters: a future edit to `admin`'s policies must not evict someone mid-incident **The `null` read is one-shot.** After any hand-write the field reads `[]` forever, and only `terraform apply -replace` restores `null` — so on a re-run, after the previous run's cleanup, this row's first clause will not reproduce. Re-runs check the survival clauses only, or recreate the group first | deterministic check (`member_entity_ids` is null on a pristine group; hand-added member survives a no-op apply AND a real group update) | 100% |
| **The map ships empty** | Read `roles.tf`, then `vault list identity/group/name` | No `app-*` entry in the map and none live. Naming a Grafana or MinIO group here is the failure this ticket was rewritten to avoid | deterministic check (zero `app-*` in the map and in Vault) | 100% |
| **One map entry produces a group** | Add `app-probe-reader` to the map and apply | The group exists with `policies = []` | deterministic check (group exists, `policies == []`) | 100% |
| **The extension point wires a real login** | Confirm `vault_identity_oidc_assignment.smoke.group_ids` **concats** the marked local onto the existing smoke group id, apply, then create a scratch entity with a `userpass` alias, put it in `app-probe-reader` **and nothing else**, and complete an OIDC authorization | It succeeds. For the control, unwire **only the assignment** and keep the scratch entity in the group: that attempt must fail with `access_denied: identity entity not authorized by client assignment`. **Do not remove the member as part of unwiring** — the denial would then have two possible causes and prove neither. A `concat` rather than a replacement matters too: the map ships empty, so replacing `group_ids` writes `[]` and breaks F2's live smoke client | deterministic check (authorization succeeds when wired; denied when only the assignment is unwired, member still present) | 100% |
| **The group name reaches the token, decoded** | Take the ID token from that login, base64-decode the payload, inspect `groups` | A JSON **array** containing `app-probe-reader` — not the string `"null"`, not a quoted string holding a bracketed list | deterministic check (decoded `groups` is an array containing the name) | 100% |
| **An application user reads nothing** | From the scratch entity's session: `vault kv get -mount=secret default/grafana/admin`; `vault kv list -mount=secret default`; `vault read sys/policies/acl/developer`; `vault list identity/entity/id` | All four denied. The claim is "no access to this cluster's secrets or config", not literally nothing — `default` still allows `cubbyhole/*`, self-lookup, `sys/wrapping/*` and the OIDC authorize path, which is why login works at all | deterministic check (all four non-zero, "permission denied") | 100% |
| **The smoke client still works after the `oidc.tf` edit** | Complete a smoke-client login as the operator entity, before and after Requirement 5's change | Both succeed. That one line is live OIDC wiring and a wrong edit breaks the existing client — the only irreversible-ish part of this ticket | deterministic check (smoke login succeeds after the edit) | 100% |
| **Everything the proof created is removed** | Remove the map entry, the scratch entity, its alias and its `userpass` user; apply. **Then remove the `admin` member by hand** — `vault write identity/group/name/admin member_entity_ids=` with an EMPTY value. **Not `member_entity_ids='[]'`**: the CLI does not JSON-parse a `k=v` value, so `[]` arrives as a two-character string and Vault answers `Code: 500 ... invalid entity ID "[]"` while the member survives. Measured. The empty form keeps `policies` and `name` intact and leaves `terraform plan` clean | No `app-*` group, no scratch objects, map empty, **and `admin` has no member**. The hand-removal is not optional and no apply will do it for you: `external_member_entity_ids = true` means Terraform never touches that field, so the member the previous row added stays forever. Leaving it ships a standing `admin` membership, which is the failure this plan ranks second-worst | deterministic check (all absent, map empty, `admin` membership empty or null) | 100% |
| **Guardrail: the denial row can fire** | Negative control: run row 8's four probes against a `developer` session (`F11`) | All four succeed | deterministic check (all four succeed) | 100% |
| **Guardrail: the claim row can fire** | Negative control: decode a token for the **operator** entity, which is in the smoke group and in no `app-*` group | Its `groups` array lacks `app-probe-reader`. Use the operator entity specifically — a zero-group entity renders `"null"` and passes the row for the wrong reason | deterministic check (operator's token decodes, `groups` lacks the name) | 100% |
| **The layout is written down and the repo gives one answer** | Read `docs/cluster-roles.md` and the revised `docs/vault-human-auth.md:71-87` | The new doc names all four roles, what each grants, which ticket defines it, the `app-<service>-<level>` convention, that a group needs an assignment to grant anything, and that neither `developer` nor `admin` is a containment boundary. **It also says that `admin` membership is not managed by Terraform, so leaving yourself in it is invisible to every plan — checking it is a habit, not a gate.** The revised section no longer tells every consumer to create its own group unconditionally. It does not decide per-app versus per-resource, and does not claim what any consumer does | model + rubric (adversarial review agent) | 4/5 |

signed-off-by: JasperHG90 2026-08-01
