eval: G1-grafana-native-oidc-login

Grafana authenticates humans against Vault's `lab` OIDC provider through its
own generic OAuth client, with no proxy in front, and every SSO user lands as
an Editor. The local admin account survives as the break-glass path.

Row methods reflect what this environment can reach. The devcontainer reaches
the cluster read-only: the operator's Vault token carries `default` only, so
`terraform apply` and any browser login are the operator's to run. Rows that
need the deployed state carry `verdict: pending-operator`.

| Behavior | Input | Expected | Fails-when | Scorer | Threshold |
|----------|-------|----------|------------|--------|-----------|
| A human signs in to Grafana through Vault, end to end | A browser with no Grafana session at `https://grafana.lab.orangecluster.nl`, clicking the Vault SSO button and completing the Vault login as `operator` | The browser lands on the Grafana home dashboard as a named, non-anonymous user. A first attempt from a browser holding no Vault UI session may fail with a generic "Failed to sign in with SSO" and succeed on retry — that is the known Vault-UI-path quirk (`docs/vault-human-auth.md:324-330`), not a defect of this ticket | The retry also fails, or the error reads `identity entity not authorized by client assignment` — that is a genuine refusal, meaning the client's assignment does not admit the entity | Human (operator, browser) | Pass/fail; verdict: pending-operator |
| The `email` claim actually reached Grafana | After the sign-in in row *A human signs in to Grafana through Vault, end to end*: `GET http://192.168.2.47:3000/api/org/users` with the local admin's basic auth | The SSO-created user's `email` field is present and non-empty, and matches the `email` value set in `vault_identity_entity.operator`'s metadata | The `email` field is empty, or holds a synthesized value (the `sub` UUID, the login name, or `@localhost`) rather than the metadata email — any of those means the claim never arrived, whatever the Vault side looks like | Deterministic (HTTP + JSON field check) | 100%; verdict: pending-operator |
| One read-only probe confirms four client settings at once | `GET https://grafana.lab.orangecluster.nl/login/generic_oauth` with no `code` parameter, following no redirects | A `302` whose `Location` header carries: `auth_url` = `https://vault.lab.orangecluster.nl/ui/vault/identity/oidc/provider/lab/authorize`, this ticket's `client_id`, `redirect_uri` = `https://grafana.lab.orangecluster.nl/login/generic_oauth`, and a `scope` parameter containing both `openid` and `email` | The `scope` omits `openid` (Vault hard-rejects the authorize request), the `redirect_uri` carries the node IP `192.168.2.47:3000` instead of the edge hostname, or the response is a `200` login page rather than a `302` | Deterministic (HTTP + `Location` header parse) | 100%; verdict: pending-operator |
| The local-admin fallback still works after OIDC lands | A browser at `https://grafana.lab.orangecluster.nl/login`, signing in with `admin` and the KV2 password from `default/grafana/admin` | The login succeeds and reaches the home dashboard, proving Vault being sealed would not lock the operator out of monitoring | The username/password form is absent from the login page, or the credentials are refused | Human (operator, browser) | Pass/fail; verdict: pending-operator |
| Guardrail: an unauthenticated request is still refused | `GET https://grafana.lab.orangecluster.nl/` with no cookie and no credentials, following no redirects | A `302` to `/login` — the pre-ticket behavior, unchanged | Any `200` serving dashboard content, i.e. the change opened anonymous access | Deterministic (HTTP status + `Location`) | 100%; verdict: pending-operator |
| Guardrail: both copies of the root URL changed | `grep -n '192\.168\.2\.47:3000' deployments/infrastructure/services/grafana.hcl deployments/infrastructure/services.tf` | Zero matches. `grafana.hcl:70` (`GF_SERVER_ROOT_URL`, the single input Grafana derives its redirect URI from) and `services.tf:444` (`grafana_external_url`, which only fills the "Open Grafana" link in the Telegram alert template) both read `https://grafana.lab.orangecluster.nl` | Either file still carries the node address. Missing `services.tf:444` does not break login, so no other row catches it — it ships alert messages pointing at an address the edge no longer matches | Deterministic (grep) | 100% |
| The SSO-created user has the Editor role | After the sign-in in row *A human signs in to Grafana through Vault, end to end*: the same `GET http://192.168.2.47:3000/api/org/users` request that row *The `email` claim actually reached Grafana* makes | The SSO user's `role` field reads `Editor` | The role reads `Viewer` — Grafana's `auto_assign_org_role` default, which is what a missing `GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH` silently produces while every other row in this marker still passes | Deterministic (HTTP + JSON field check) | 100%; verdict: pending-operator |
| The client id reached the provider's allowlist | `terraform -chdir=deployments/infrastructure validate` plus a read of `local.oidc_provider_client_ids` in `deployments/infrastructure/oidc.tf` | The list carries `vault_identity_oidc_client.grafana.client_id` alongside the four existing entries | The entry is absent. Vault gates the provider on this list, offers no standalone resource for it, and reports no Terraform error — the authorization request is simply refused at login | Deterministic (HCL read + `terraform validate`) | 100% |
| Guardrail: no client secret landed in a `.tf` file | `grep -rn 'hvo_secret_' deployments/` and a read of the Grafana client-secret path | Zero matches. The secret reaches Grafana only through KV2 and the existing `template { env = true }` block | Any literal `hvo_secret_...` in a tracked file. `detect-private-key` (`.pre-commit-config.yaml:12`) will not catch this — it matches a fixed list of PEM headers, and a Vault client secret is not one of them | Deterministic (grep) | 100% |
| `terraform plan` is additive-only | `terraform -chdir=deployments/infrastructure plan` against this ticket's changes | The plan adds the OIDC client, its key registration and the KV2 secret, and updates `nomad_job.grafana` in place. It destroys and replaces nothing | The plan shows a destroy or a replace of any existing resource — in particular of `vault_identity_oidc_key.lab` or an existing client, whose `client_type` and `key` are immutable after create | Deterministic (`terraform plan` output) | 100% (verdict: assessed-statically-not-executed — `terraform plan` needs a broker token with the deploy role, and this session's Vault token carries `default` only; verified instead by `terraform validate` on the infrastructure root, offline and passing, plus a manual read of the diff confirming only additions and one in-place job update) |

signed-off-by: JasperHG90 2026-09-02
plan: 0574f8a141b40982b75b88b0de818053db4249d4337ff96f23b8a5708c673fba
