eval: G2-nomad-ui-oidc-login

**Forks resolved 2026-07-31.** Q1 the `developer` policy is **referenced by
name, never authored and never imported** — corrected 2026-07-31 from an
earlier "imported into Terraform", which rested on a false premise. **Ansible
owns this policy** (`bootstrap/roles/nomad_server/tasks/main.yml:182-187`
applies `bootstrap/roles/nomad_server/files/nomad_developer_policy.hcl` with
the bootstrap token), so an import would give one policy two owners and they
would fight on every `just bootstrap`. Q2 `token_locality = "global"` and
`max_token_ttl = 8h`. Q3 F11's live `developer` Vault group. Q4 the
callback paths get verified against the running UI before they are written
into the Vault client.

**Definition of Done:** Nomad is a registered OIDC client of Vault's `lab`
provider, and a developer who is a member of the bound Vault group can sign
in to the Nomad web UI at `https://nomad.lab.orangecluster.nl` and to
`nomad login` from a terminal, receiving in both cases an ACL token that
carries the `developer` policy and nothing more. A developer outside that
group is refused. The `developer` Nomad policy is consumed by name and this
ticket authors no copy of it.

**The trap this marker guards.** F2 shipped a defect of exactly this shape and
caught it only because a gate decoded the token: Vault drops a `groups` claim
it cannot render and **still signs and returns the token**. Nothing errors.
The relying party then sees a valid login with no groups, the binding rule
matches nothing, and the login fails with `400 no role or policy bindings
matched` — measured on Nomad 2.0.4; no token is issued, and a green login
carrying an empty policy list is unreachable. Rows 2 and 3 exist because a row
that merely asserts "a token came back" is now redundant against *this*
defect — a mismatched selector reds it by itself. Rows 2 and 3 earn their
place against a different one: a login that succeeds and returns a token
carrying the wrong policies, which no status code reveals.

**Second trap: the wrong provider works.** Vault's built-in `default` provider
advertises `allowed_client_ids = ["*"]`. An auth method pointed at it
completes a login successfully, over plaintext HTTP against a raw IP, while
bypassing every scoping decision F2 made. Row 6 is a positive check on the
issuer for that reason; a login succeeding is not evidence it was scoped.

| Behavior | Input | Expected | Scorer | Threshold |
|----------|-------|----------|--------|-----------|
| The auth method exists and is type OIDC | `nomad acl auth-method list` and `nomad acl auth-method info <name>` | Exactly one OIDC auth method. Before this ticket the cluster has **none** (verified 2026-07-31), so its presence is attributable to this change | deterministic check (exactly one OIDC auth method present) | 100% |
| **A real UI login yields a token carrying `developer`** | Complete the browser flow at `https://nomad.lab.orangecluster.nl` as a member of the bound group, then `nomad acl token self`, reading only its `Type`, `Global` and `Policies` lines (the bare output prints the Secret ID in full, so filter it) with the issued token | Login completes AND the token's `Policies` list contains `developer`. **A selector that matches nothing does not yield an empty-policy token — it yields no token at all**: measured on Nomad 2.0.4, the login returns `400 no role or policy bindings matched`, guarded in both the OIDC and JWT paths of `acl_endpoint.go`. So this row's failure signature is a failed login, not a green one with an empty list | deterministic check (token issued; `Policies` contains `developer`; list non-empty) | 100% |
| **The `groups` claim actually arrives, decoded or proven** | Either decode the `id_token` Vault issues to this client and inspect its payload, **or** complete a real login and read the issued Nomad token's `Policies` | **Preferred: the decoded payload has a `groups` claim that is a JSON array containing the group name**, not the string `"null"` and not a quoted string, since a malformed scope template renders `{"groups": "null"}` and still issues a valid token. **Accepted alternative, because Nomad consumes the `id_token` internally and never surfaces one**: a completed login whose Nomad token carries `developer`. That is decisive rather than suggestive — the binding rule selector is `"developer" in list.groups`, and a missing or malformed claim yields `400 no role or policy bindings matched` and NO token, measured on Nomad 2.0.4 and guarded in both the OIDC and JWT paths of `acl_endpoint.go`. A green login with an empty policy list is unreachable, so a token carrying `developer` proves the claim arrived as a real list. Decoding directly requires a browser round trip with the client secret; record which path was used. **AND, either way, read `oidc_scopes` off the auth method: it must be exactly `groups`.** R1 settled that on 2026-08-01 — Nomad adds `openid` itself, so listing it is redundant, and an earlier build shipped `["openid", "groups"]` against a signed marker that forbade it. No other row checks this field | deterministic check (a `groups` array in the payload, or a login yielding a token whose `Policies` contains `developer`; plus `oidc_scopes` equal to `groups`) | 100% |
| **A non-member is denied (guardrail)** | A scratch Vault entity NOT in the bound group attempts the same UI login. Delete it afterwards | Authorization is REFUSED and no Nomad ACL token is issued. The control must be shown to discriminate: the scratch entity must **first log in to Vault successfully and carry a non-empty `entity_id`**, so the refusal is attributable to the assignment rather than to a broken login | deterministic check (Vault login succeeds with non-empty entity_id; Nomad authorization refused; no token) | 100% |
| `nomad login` works from a terminal | `nomad login -method=<name>` on a machine with a browser, using the default callback `localhost:4649` | A token is returned carrying `developer`. This is a separate row from the UI because it exercises the **second** redirect URI: omitting `http://localhost:4649/oidc/callback` from either `allowed_redirect_uris` or the Vault client's `redirect_uris` breaks CLI login while leaving the UI working, so a UI-only check reports green on a half-broken ticket | deterministic check (`nomad login` returns a token carrying `developer`) | 100% |
| **The auth method points at `lab`, not Vault's `default` provider** | Read `oidc_discovery_url` and `bound_issuer` out of the auth method's nested `config` block | Both resolve to `https://vault.lab.orangecluster.nl/v1/identity/oidc/provider/lab`. Note `bound_issuer` is typed `["list","string"]` in provider 2.5.2, so it holds that issuer as a one-element list, not a bare string. HTTPS and the `lab` path are both load-bearing. A method aimed at `default` logs in successfully while bypassing the provider's `allowed_client_ids`, so this cannot be inferred from a working login | deterministic check (discovery URL and bound issuer both the `lab` HTTPS issuer) | 100% |
| PKCE is enabled | Read `oidc_enable_pkce` off the auth method's `config` block | `true`, required by this ticket as **defense in depth** rather than by Nomad. It is achievable here because Vault does its half: the live `lab` discovery document advertises `code_challenge_methods_supported: ["plain","S256"]`, and the cluster runs 2.0.4, past 1.10.0. **Rationale corrected 2026-07-31**: an earlier version of this row said PKCE is "required from Nomad 1.10 per HashiCorp's Vault-to-Nomad guide". That guide never mentions PKCE. PKCE is *supported* from v1.10.0 (`nomad/docs/secure/authentication/sso-pkce-jwt`: "Beginning with Nomad v1.10.0, Nomad supports PKCE"), the 1.10.0 changelog says Nomad "enables" it, and the provider schema types the field optional and off by default. The row scores the setting, not the justification | deterministic check (`oidc_enable_pkce == true`) | 100% |
| The client is registered in both places Vault requires | `terraform state list` plus `vault read identity/oidc/provider/lab` | A `vault_identity_oidc_key_allowed_client_id` exists for the Nomad client AND the client id appears in the `lab` provider's `allowed_client_ids`. These are two separate registrations and omitting the provider one makes Vault refuse the authorization request. Also assert the key resource carries **no inline** `allowed_client_ids` | deterministic check (standalone key registration present; client id in provider list; no inline attribute on the key) | 100% |
| **Guardrail: no client secret in the diff, scored by something that can fail** | `git diff <base>..HEAD -- '*.tf' '*.tfvars' 'bootstrap/**'` piped to `grep -nE -e 'client_secret[[:space:]]*=[[:space:]]*"' -e 'hvo_secret_'` | No match. **Do NOT score this with `detect-private-key`**: it matches a fixed blocklist of PEM headers and passes against a literal Vault client secret. F2's marker records that exact false green **Pattern corrected 2026-07-31: the alternation had been written with a backslash before it, which `grep -E` reads as a literal character rather than an alternation, so the guardrail searched for one long string instead of two patterns and could never match.** It now uses two `-e` patterns, which needs no alternation at all. Verified vacuous against a file containing both leak shapes. This is the same false-green class the row was written to prevent. | deterministic check (pattern match over the diff, expected empty) | 100% |
| **The `developer` policy is consumed, never re-authored** | `terraform state list` filtered for `nomad_acl_policy`, `grep -rn 'resource "nomad_acl_policy"' deployments/`, `grep -rn --include=*.tf '^import' deployments/`, and `nomad acl policy list` | **No `nomad_acl_policy` resource named `developer`**, anywhere, and no `import` block for it. Exactly one live `developer` policy. **Match on the policy NAME, not the resource type**: `nomad_acl_policy.deploy` legitimately exists at `nomad_deploy_role.tf:13` until F8 moves it, so a check that reds on "any `nomad_acl_policy` in state" fails correct work. Ansible owns `developer` (`nomad_server/tasks/main.yml:182-187`, source `files/nomad_developer_policy.hcl`); importing it would give one policy two owners, and the next `just bootstrap` would start a fight that never ends | deterministic check (no `nomad_acl_policy` whose name is `developer`; no import block; exactly one live `developer` policy) | 100% |
| **Both Nomad ACL resources are in Terraform, and Ansible is untouched** | `grep -rn 'resource "nomad_acl_auth_method"' deployments/`, the same for `resource "nomad_acl_binding_rule"`, then `git diff <base>..HEAD --stat -- bootstrap/` | **One declaration each under `deployments/infrastructure/nomad_oidc.tf`, and `bootstrap/` is NOT touched.** The ticket was replanned on 2026-08-02: a Vault Nomad role with `type = "management"` brokers a token that writes both objects, measured live, so there is no playbook and no new `just` recipe. Match `resource "..."` declarations, not bare mentions — the plan records in prose why the CLI form is not idempotent | deterministic check | 100% |
| **Guardrail: existing access is unbroken** | After apply, two separate probes. Brokering: `vault read nomad/creds/deploy`, then `nomad acl token self`, reading only its `Type`, `Global` and `Policies` lines (the bare output prints the Secret ID in full, so filter it) with it. Workload path: `vault auth list` and `vault read auth/jwt-nomad/config` | The brokered `deploy` token still mints and still reports `Type = client`, `Policies = [deploy]`. And `vault auth list` still shows the `jwt-nomad/` mount of type `jwt`, whose config still carries `default_role = nomad-workloads` and `jwks_url = http://127.0.0.1:4646/.well-known/jwks.json` (values as of 2026-07-31). **Input corrected 2026-07-31: an earlier version of this row probed the workload path with `nomad acl auth-method list`, which cannot observe it at all** — `jwt-nomad` is a **Vault** auth mount, not a Nomad auth method, so it can never appear in that listing. Worse, after this ticket that command returns exactly the new OIDC method, which a scorer could misread as a green signal about something it never looked at. This ticket is additive; a regression here means it changed the machine paths while aiming at the human one | deterministic check (deploy token mints and is unchanged; `jwt-nomad` Vault mount present, type `jwt`, config unchanged) | 100% |
| **The `alloc-node-exec` grant is real, and proven rather than described** | With a token issued by this auth method: `nomad alloc exec <alloc> /bin/sh -c id` against a live allocation, and a node-level exec | **Both succeed** — and that is the point of the row, not a failure. The live `developer` policy grants `alloc-exec` and `alloc-node-exec` (`nomad_server/files/nomad_developer_policy.hcl`), so everyone this ticket admits gets shell inside allocations and on the node. Run it so the capability is demonstrated rather than assumed, then clean up. **Do not score this by reading the implementer's close-out** — a reviewer cannot verify that an operator accepted anything, and a row whose only evidence is the implementer's own prose guards nothing | deterministic check (both exec probes succeed and are recorded; scratch allocation cleaned up) | 100% |
| The widening is written down where a reader will meet it | Read the docs this ticket ships | They state that signing in through Nomad's UI grants the `developer` Nomad policy, and that this includes `alloc-exec` and `alloc-node-exec` — shell inside any allocation and on the node itself. It must not be described as read-only or as "developer access" without naming what that includes. Whether `alloc-node-exec` belongs in the policy at all is recorded as open in the roadmap and is not this ticket's call | model + rubric (adversarial review agent) | 4/5 |
| **The rebuild path gains no new step** | `grep -c 'nomad_oidc' README.md` and read the three-step rebuild sequence in `README.md` | **Zero matches, and the sequence still reads bootstrap, infrastructure, applications.** R6c requires exactly this: everything this ticket adds ships inside the infrastructure `terraform apply`, so there is nothing for a rebuild to skip. An earlier draft added a fourth step for a standalone recipe that no longer exists | deterministic check | 100% |
| **The brokered management token exists, is scoped, and is read fresh every run** | `grep -n 'type *= *"management"' deployments/infrastructure/nomad_oidc.tf`, `grep -n 'nomad/creds/manage' deployments/infrastructure/developer_group.tf`, then `vault read -field=secret_id nomad/creds/manage` and, under that token, `nomad acl token self` reading only its `Type` line | The role declares `type = "management"`, `developer_group.tf` carries a `nomad/creds/manage` read grant, and the brokered token reports `Type = management`. **The data source must carry NO `depends_on`** — an earlier revision ordered it after `vault_policy.developer`, which deferred the read to apply time and made every subsequent apply 403 while refreshing the auth method with the previous run's expired token. Do not score that with a bare `grep depends_on`: the file explains the decision in comments, so the word appears in prose. Check the `data "vault_nomad_access_token"` block itself. The grant must PRE-EXIST rather than be ordered; today that is free because this root runs as root | deterministic check (the two greps hit, the brokered token is management, and the data source block contains no `depends_on` argument) | 100% |
| **A second apply is a no-op and leaves exactly one binding rule** | `terraform apply` twice, then `nomad acl binding-rule list` | The second apply reports no changes, and the method carries **exactly one** rule. Assert exactly one, not presence: a presence check passes against an already-duplicated state, which is the failure it exists to catch. The CLI form grows one rule per run and `binding-rule list` returns no `Selector` and has no `-auth-method` flag, so the guard is unwriteable outside Terraform — this row is why the resource owns it | deterministic check | 100% |
| The repo gate passes | `just worktree_setup <path>`, then `just pre_commit` | All Passed. This change is Terraform only — the Vault client and assignment, the management role, and both Nomad ACL resources all live in `deployments/infrastructure/nomad_oidc.tf` — so `terraform fmt -check` and `terraform validate` per root are what bite | deterministic check (`just pre_commit` all Passed) | 100% |

signed-off-by: JasperHG90 2026-08-02
