eval: G3-alerts-standalone-telegram-bot

Grafana alerts reach OrangeClusterAlertBot and nothing else, and the repo says
so truthfully: what is scoped, what enforces it, and what does not.

| Behavior | Input | Expected | Fails-when | Scorer | Threshold |
|----------|-------|----------|------------|--------|-----------|
| The repo no longer claims the alert chat borrows the Hermes bot's token | `grep -n "reuses Hermes" deployments/infrastructure/variables.tf`, then read the whole `telegram_alert_chat_id` block | The grep exits 1 with no output. The rewritten `description` states both properties the value carries: it is the operator's own Telegram user ID, and it is the entire outbound scope of the alert bot. It names neither Hermes nor its Vault path | The description is reworded but still ties the alert chat to the Hermes bot, or drops one of the two properties and reads as a bare label like "chat ID for alerts" | Deterministic (grep for the stale phrase) plus a human read of the replacement | 100% on the grep; the human read must find both properties present |
| Guardrail: the tracked example file teaches the variable without publishing the operator's real ID | `grep -n telegram_alert_chat_id deployments/infrastructure/vars/prod.tfvars.example` | Exactly one line, carrying a placeholder in the style of `deployments/applications/vars/prod.tfvars.example:6` (`"0000000000"`). The real value `10650075` appears nowhere in any file tracked by git under `deployments/*/vars/` | The line is absent, so a fresh checkout never learns the variable exists; or the real ID `10650075` is pasted into the example, publishing a personal identifier in a tracked file | Deterministic (`grep` for the variable, plus `git grep -n 10650075 -- 'deployments/*/vars/'` returning nothing) | 100% |
| The change moves no Terraform state | `terraform plan` in `deployments/infrastructure`, with live Vault and Nomad credentials | Reports `0 to add, 0 to change, 0 to destroy`. In particular `nomad_job.grafana` is absent from the plan, proving the `###` comments landed in the `templatefile` argument map and not in the jobspec body | The plan lists `nomad_job.grafana`, which means a comment reached the `.hcl` and the job would be re-registered on apply | Deterministic (plan output), operator-run | 100%. `verdict: pending-operator` |
| Guardrail: the doc credits each half of the scoping to what actually enforces it | Read the new `### Where alerts go` section of `docs/monitoring.md` | Outbound scoping is attributed to the single `chatid` in the contact point. Inbound is attributed to the absence of any reader of the bot's updates. BotFather's group switch is described as stopping the bot being added to groups, and nothing more | The section says or implies that a BotFather setting stops strangers messaging the bot, for example "groups are off, so only you can message it". Telegram's own bot features page states the opposite: "All bots must be able to process direct messages" | Deterministic floor (`grep -niE "botfather" -A4 docs/monitoring.md` must not co-occur with a "only you can message"/"cannot message" claim) plus a human read, which is the real scorer | 100%. A single false attribution fails the row |
| The doc names the precondition that makes delivery work at all | `grep -n "403\|Start" docs/monitoring.md`, then read the surrounding lines | The section states that the operator must open the chat with the bot first, because Telegram forbids a bot from starting a conversation, and names `403 bot can't initiate conversation with a user` as the symptom when that has not happened | The section documents the routing and the scoping but omits the precondition, so a future bot swap looks correctly configured while delivering nothing | Deterministic (grep for the 403 string) plus a human read for the explanation around it | 100% |
| A firing alert arrives in OrangeClusterAlertBot, and the Hermes bot stays silent | Press **Test** on the `telegram-default` contact point in Grafana, or let a real alert fire | The notification appears in the OrangeClusterAlertBot chat, formatted by the `telegram.message` template (severity header, alert name, instance, the "Open Grafana" link). No copy of it appears in the Hermes bot chat | The message arrives in the Hermes chat, meaning the Vault value at `default/grafana/telegram` was not actually swapped; or nothing arrives anywhere, meaning the chat was never opened or the token is wrong | Human (the operator reads their own Telegram) | 100%. `verdict: pending-operator` |

signed-off-by: Jasper 2026-09-06
plan: 1afea15f015ad6390c7aff5aabcb2b35e216e0c91246874e778060118ded915e
