eval: L4-landing-dash-fe-be-split

`dash` splits into a frontend task (static files only, zero Python) and a
backend task (status computation) in the same Nomad job. The backend holds
its own copy of the narrow health/join/consul logic it needs, with zero
runtime dependency on the `cli` package, and `/api/status`'s contract and
the credential-never-reaches-frontend guarantee both survive the split
unchanged.

| Behavior | Input | Expected | Fails-when | Scorer | Threshold |
|----------|-------|----------|------------|--------|-----------|
| The backend has zero `cli` dependency | A repo-wide grep for `localstack_cli` and `localstack-cli` scoped to `deployments/applications/services/dash/backend/` (source, tests, and `pyproject.toml`/`uv.lock`) | Zero matches | Any match — an import, a `[tool.uv.sources]` entry, or a `localstack-cli` line in `pyproject.toml`/`uv.lock` | Deterministic (grep) | 100% |
| Every copied module names its real source | `health.py`, `nomad_client.py`, `consul_client.py`, `services.py` under the backend's tree | Each carries a comment naming the exact `cli/src/localstack_cli/api/*.py` file it was copied from | A copied module with no provenance comment, or one citing the wrong source file | Deterministic (grep + manual read) | 100% |
| Status computation is unchanged after the import rewire | The ported `test_status.py` suite (Health-enum mapping across all four values, UNKNOWN-on-no-matching-job, agent-endpoint Consul-check fallback) run against the new local `health.py`/`services.py`/`consul_client.py` | All assertions pass unchanged from L3's own suite; only import paths differ | Any assertion's expected value changes, or a case from L3's suite is silently dropped | Deterministic (unit test) | 100% |
| The new HTTP client behaves like `cli`'s did | `respx`-mocked calls to the new `_http.py`/`nomad_client.py`, covering a token-bearing request and a paginated `job_statuses` response (`X-Nomad-Nexttoken`) | The token lands in Nomad's own header (not a generic `Authorization` header); pagination is followed until the header is empty, matching `cli/tests/test_api_nomad.py`'s own assertions | The token header is wrong, or a second page is never fetched | Deterministic (unit test, respx) | 100% |
| `/api/status`'s JSON contract is unchanged | The same fixture tile config and fake Nomad/Consul data L3's `test_main.py` used, run against the split backend | The response JSON is byte-for-byte identical in shape (same keys, same per-tile fields) to what L3's backend produced for the same input | Any key added, removed, or renamed in the response | Deterministic (unit test, snapshot/diff) | 100% |
| Connect-info responses still never carry a credential value | The rendered `/api/status` response for all 5 backend-row tiles, post-split | Only protocol/host/port/auth-method/example text appears; no password, token, or API key value, matching L3's own guardrail | Any backend tile's response embeds a literal secret value | Deterministic (pattern check + code-path read) | 100% |
| The frontend carries nothing it doesn't need | The built frontend image / task definition | No Python interpreter, no `cli`, no Vault/Nomad credential file, no `tiles.json` mount, no `vault {}` stanza | Any of the above present in the frontend task or image | Deterministic (image inspection + `dash.hcl` read) | 100% |
| Two tasks, one job | `deployments/applications/services/dash.hcl` | Exactly `task "frontend"` and `task "backend"` inside one `group "dash"` — not two `job` blocks | A second `job` block, or the two tasks in separate groups | Deterministic (HCL read) | 100% |
| Only the backend is vaulted | `dash.hcl`'s two task bodies | `vault { role = "dash" }` appears once, on `task "backend"`; `task "frontend"` has no `vault {}` stanza at all | The frontend task carries a `vault {}` stanza, or the backend's is missing/renamed | Deterministic (HCL read) | 100% |
| The Nomad ACL grant is untouched | `deployments/infrastructure/nomad_dash_read_role.tf` before and after this ticket's diff | Zero diff — same `read-job`/`list-jobs`/`node:read` capabilities, same role name | Any change to the file's resources or `rules_hcl` | Deterministic (`git diff`) | 100% |
| No `haproxy.hcl` changes | The full diff this ticket produces | Zero lines touched in `deployments/infrastructure/services/haproxy.hcl` | Any hunk touches that file | Deterministic (`git diff --stat`) | 100% |
| The retargeted pre-commit hooks fire correctly | (a) A trivial lint violation under `dash/backend/`, `pre-commit run --files <that file>`; (b) a content-only change under `dash/frontend/` (no Python), same command | (a) The four `dash-backend-*` hooks execute and catch the violation; (b) none of the four Python hooks fire (nothing to lint) | (a) shows zero hooks executed; (b) a hook fires anyway (a stale `files:` pattern still matching the old path) | Deterministic (pre-commit output) | 100% |
| Both image tags are pinned | `dash.hcl`'s two `config.image` values | Concrete tags, never `:latest` and never an implicit-latest bare reference | Either image reference ends in `:latest` or carries no tag | Deterministic (grep) | 100% |
| `terraform plan` is additive-only on both roots | `terraform -chdir=deployments/applications plan` and `terraform -chdir=deployments/infrastructure plan`, run against this ticket's changes | `applications` shows the `dash` job's spec changed (two tasks) and destroys nothing; `infrastructure` shows the `oauth2_proxy` job updated in place (new multi-upstream value) and destroys nothing | Either plan shows a destroy or replace of an existing resource | Deterministic (terraform plan output) | 100% (verdict: assessed-statically-not-executed — `terraform plan` needs the operator's own authenticated cluster session (`localstack env`), unavailable to an unattended agent; verified instead via `terraform validate` on both roots, offline, passing, plus a manual read of the diff confirming no resource is removed, only `nomad_job.dash`'s spec and `nomad_job.oauth2_proxy`'s env template change in place) |
| The live route still answers after the split | `GET` `https://dash.lab.orangecluster.nl` (frontend) and its `/api/status` path (routed to the backend) through oauth2-proxy, post-deploy | The page loads from the frontend task; `/api/status` returns live tile data from the backend task, same shape as before the split | Either route 404s, times out, or `/api/status` returns stale/hardcoded-looking data | Human (operator, one-time post-deploy check) | Pass/fail; verdict: pending-operator |

signed-off-by: operator (delegated — "I'm not here so drive the loop yourself. You are in control.") 2026-08-24

amended-by: operator (delegated — "I'm not here so drive the loop yourself. You are in control.") 2026-08-24 (row: `terraform plan` is additive-only on both roots; reason: no live cluster session available to an unattended agent; annotated assessed-statically-not-executed, verified via offline terraform validate + manual diff read instead)
<!-- amendments: 1 (last: 2026-08-24 by operator (delegated — "I'm not here so drive the loop yourself. You are in control.")) -->
