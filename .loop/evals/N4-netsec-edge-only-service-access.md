eval: N4-netsec-edge-only-service-access

**Definition of Done:** every service with an edge route is reachable only
through `https://<name>.lab.orangecluster.nl`. Direct connections to the
service ports from a LAN host that is not a cluster node are refused, the
broad `192.168.0.0/16` rules are gone rather than shadowed, this repo's own
tooling talks to the edge, every job still runs, and a runbook exists that
gets an operator out of every failure this change makes possible.

**The trap that governs this whole marker.** Neither provisioner removes a
firewall rule. The Ansible role loops `ufw allow` and has no delete step
(`bootstrap/roles/firewall/tasks/main.yml:15-22`); the Terraform
`null_resource.firewall` is create-only, which is the defect `N3` exists to
fix. So **narrowing a rule adds a narrow one and leaves the broad one live**,
`ufw` matches the broad one, and the port stays open. A run reports `changed`,
the config reads perfectly, and nothing closed.

Therefore: **no closure in this marker may be scored by reading a config
file.** Every row asserting that something is now unreachable is scored from a
socket, attempted from a LAN host that is not a cluster node. The rows that do
read config are the guardrails, and they exist to catch the inverse error:
something reopened, or a rule keyed to a host that should not have one.

**The second trap: a test that cannot fail.** A `curl` that is refused proves
nothing on its own, because a typo in the hostname is also refused. Row 1
requires the **pre-change 200 to be recorded first**, so every closure is a
measured transition rather than an assertion about an unreachable address.

**Forks resolved 2026-07-31**, all four on their recommendations, and four
rows exist because of them. Q1 teaches the Ansible role to reconcile, so a row
now checks the role deletes an undeclared rule rather than accepting a manual
`ufw delete`. Q2 restricts 8404 to the Prometheus host, so a row asserts it is
still reachable from there. Q3 allows no devcontainer exception, so a row bars
any rule keyed to a non-cluster host. Q4 keeps the explicit host ACLs, so a
row pins the 503 on an unknown Host.

**Measured starting state, 2026-07-31.** All 200, all plaintext, all from the
devcontainer: `192.168.2.30:4646` (Nomad), `:8200` (Vault), `:8500` (Consul),
`192.168.2.29:9001` (MinIO console), `:9000` (MinIO S3),
`192.168.2.30:8404` (haproxy stats, unauthenticated), and `firebat:4646` by
mDNS name.

| Behavior | Input | Expected | Scorer | Threshold |
|----------|-------|----------|--------|-----------|
| **The negative control is captured before the change** | Before touching any rule, from a non-cluster LAN host: request each of the seven URLs in the measured-state paragraph and record status and timing | All seven recorded, and the record is committed with the ticket. Without this, every "refused" in the rows below is indistinguishable from a typo, an unplugged cable, or a host that was never listening. **This row is scored first and cannot be reconstructed afterwards** | deterministic check (seven pre-change responses recorded and committed) | 100% |
| **Direct access is refused from a non-cluster LAN host** | After the change, from the same host, the same seven URLs | Every one refused: connection refused, or timeout, or reset. **Not** a 401, 403 or 503, which would mean the packet still reached a listener and only the application declined. The distinction matters because this ticket closes the network path, not the application | deterministic check (all seven refused at the transport layer) | 100% |
| **The broad rule is DELETED, not shadowed** | `ufw status numbered` on every node in `bootstrap/inventory/cluster.ini` | **No** `192.168.0.0/16` entry survives for 4646, 8200, 8500, or `20000:32000`. A narrow rule added beside a surviving broad one is the single most likely failure of this ticket, it passes any config-file check, and `ufw` will match the broad rule. Enumerate the live table; do not infer it from the playbook | deterministic check (no broad entry for the closed ports on any node) | 100% |
| **The mDNS name is closed too, not just the IP** | `http://firebat:4646/` and `http://firebat.local:4646/` from a non-cluster LAN host | Refused. `avahi-daemon` is installed on every node (`install_dependencies.yml`), so the hostname is a second route to the same socket. The operator's own report named this form. A rule keyed on anything but the destination port and source address would close one and leave the other | deterministic check (both name forms refused) | 100% |
| **The Nomad dynamic port range is closed** | Pick a live allocation's dynamic port from `nomad alloc status`, then connect to it directly from a non-cluster LAN host | Refused. `20000:32000` is open to `192.168.0.0/16` today, which means **every dynamically-ported job is directly reachable** whether or not it has a named firewall entry. A ticket that closes only the six named services leaves the whole range open and looks complete | deterministic check (a live dynamic port is refused) | 100% |
| **haproxy stats is off the tailnet** | `http://192.168.2.30:8404/` from a tailnet address and from a non-cluster LAN host | Not reachable from either. Today it returns 200 with no auth and publishes every backend IP, port and health state, and it is the only thing besides 80/443 open to `100.64.0.0/10` (`services.tf:52-53`). Q2 resolved to restrict it to the Prometheus host and drop the human stats UI | deterministic check (8404 not served from tailnet or non-cluster LAN) | 100% |
| **8404 is still reachable FROM the Prometheus host (positive control)** | Connect to `192.168.2.30:8404/metrics` from `192.168.2.47`, the Prometheus host | 200 with metrics. Q2 kept scraping as 8404's real job (`haproxy.hcl:129` serves `/metrics` via `http-request use-service prometheus-exporter`). **Closing 8404 to everything passes the row above and is a failure**, and its only symptom is an empty Grafana panel discovered days later. Every closure row in this marker needs its matching positive control, and this is 8404's | deterministic check (200 from the Prometheus host with metrics) | 100% |
| **The Ansible role RECONCILES; it does not merely stop declaring** | Add a throwaway rule via the role, remove it from `firewall_ufw_ports`, re-run the playbook, then read `ufw status numbered` | The throwaway rule is **gone from the host**. Q1 resolved to teach the role to delete what it no longer declares, mirroring N3 on the Terraform side. **This row exists because the broad-rule row above passes against a one-off `ufw delete`**, which fixes today's rules and leaves the accumulate-only role intact so the next narrowing hits the same bug. The defect is the role, not these particular rules | deterministic check (an undeclared rule is removed from the host by a normal run) | 100% |
| **Guardrail: no rule is keyed to a non-cluster host** | Read the final rule set in `configure_network.yml` and the Terraform firewall map | Every `from_ip` is a cluster node address, `192.168.0.0/16` for port 22 and the edge ports, or `100.64.0.0/10` for port 22 and the edge. **No entry pins a developer machine, a laptop, or the devcontainer.** Q3 resolved there is no exception: such an allowance depends on a DHCP address that will change, and it silently reopens exactly what this ticket closes for whoever inherits that lease | deterministic check (no non-cluster host address in any rule) | 100% |
| **Guardrail: haproxy still refuses an unknown Host** | `curl -k -H 'Host: bogus.example.com' https://192.168.2.30/` | 503, as measured 2026-07-31. Q4 resolved to keep the ten explicit `hdr(host)` ACLs with no wildcard and no `default_backend`. A wildcard `hdr(host) -m end .lab.orangecluster.nl` plus a default backend would route unknown names somewhere instead of refusing them, inverting the point of the ticket. The list is the allowlist, and this row stops a later convenience edit from quietly removing it | deterministic check (unknown Host returns 503; no wildcard ACL; no default_backend) | 100% |
| **Postgres and the registry are knowingly untouched** | `ufw status` for 5432 and 5000 | Unchanged and explicitly recorded as out of scope in the close-out. Neither has an edge route, so "use the hostname" is meaningless for them. This row exists so the boundary is a decision on the record rather than an oversight, and so nobody silently firewalls a database port inside a networking ticket | deterministic check (5432 and 5000 unchanged; exclusion recorded) | 100% |
| **All ten edge hostnames still serve** | `https://<h>.lab.orangecluster.nl` for minio, s3, vault, nomad, consul, phoenix, memex, grafana, mlflow, bifrost | Every one answers as it did before the change, compared against the pre-change capture from row 1. The edge is the only remaining way in, so a hostname that stops working is now a total outage for that service rather than an inconvenience | deterministic check (all ten match their pre-change responses) | 100% |
| **The full API still proxies, not just the UI** | `https://nomad.lab.orangecluster.nl/v1/agent/health`, `https://vault.lab.orangecluster.nl/v1/sys/health`, `https://consul.lab.orangecluster.nl/v1/status/leader` | All 200. Verified 200 before the change. Terraform and the CLI drive the API, not the UI, so a change that preserves the web interface while breaking `/v1/` would pass a browser check and break every deploy | deterministic check (all three API paths 200 through the edge) | 100% |
| **The cluster is unchanged underneath** | `nomad node status`, `nomad job status`, `vault status`, `consul members`, and an allocation census | 5 nodes ready, 19 jobs running, 24 allocations with none pending or failed, Vault unsealed on Consul storage, Consul quorum intact. These are the pre-change numbers. Intra-cluster traffic uses ports this ticket does not touch, so a regression here means it closed something it did not intend to | deterministic check (node, job, alloc, seal and quorum state all match pre-change) | 100% |
| **Prometheus is still scraping** | Query Prometheus for scrape health across its targets | No target that was up before is down after. Prometheus scrapes node exporters and the haproxy stats endpoint across hosts, so it is the most sensitive consumer of narrowed rules, and its failure is silent until a dashboard is empty days later | deterministic check (no target transitions up to down) | 100% |
| **Both Terraform roots plan clean from the devcontainer** | `just init` then a plan in `deployments/infrastructure` and `deployments/applications`, using the migrated addresses | Both exit 0 with no error. This is the real proof the address migration worked, because it exercises the Vault, Nomad and Consul providers over the edge in one go. Key presence is not proof; a clean plan is | deterministic check (both roots plan, exit 0) | 100% |
| **No raw service address survives in the repo** | `grep -rnE '192\.168\.2\.(29\|30\|46\|47\|50):(4646\|8200\|8500\|9000\|9001)' --include='*.tf' --include='*.env*' --include='*.hcl'` outside `services/haproxy.hcl` and the firewall map | No match. `deployments/applications/providers.tf:40` hardcodes `192.168.2.30:8500` today and must move. haproxy's own backend definitions and the firewall map are the two legitimate places a raw IP belongs, since they are what the addresses resolve to | deterministic check (no raw service address outside the two allowed files) | 100% |
| **`.devcontainer/.env.example` teaches the new world** | Read it | `VAULT_ADDR`, `NOMAD_ADDR` and `CONSUL_HTTP_ADDR` are HTTPS edge hostnames. The template is what the next person copies; leaving it on `http://192.168.2.30:8200` hands them a config that cannot work and looks official | deterministic check (all three are HTTPS edge hostnames) | 100% |
| **SSH is untouched on every node** | `ufw status` on all five nodes, plus an actual SSH connection from LAN and from tailnet | Port 22 still allowed from `192.168.0.0/16` **and** `100.64.0.0/10`, and both connections succeed. This is the recovery path for every failure this ticket creates. A change that closes it converts a service outage into physical access | deterministic check (22 open from both ranges; both connections succeed) | 100% |
| **The recovery runbook exists and every command in it works** | Run each command in `docs/edge-and-recovery.md` against the live cluster | Every command executes and does what the runbook says. A runbook is worth nothing if it was written from memory: the failure it covers is exactly when nobody has the patience to debug a typo in the fix | model + rubric (adversarial review agent) | 5/5 |
| **The runbook answers the circular dependency by name** | Read `docs/edge-and-recovery.md` | It states plainly that haproxy is a Nomad job, that after this change the only route to Nomad's API is haproxy, and therefore that a dead haproxy cannot be restarted through the edge. It gives the escape: SSH to firebat, then `NOMAD_ADDR=http://127.0.0.1:4646 nomad job restart haproxy`. **This is the failure this ticket creates**, and an operator who has to derive it during an outage is being asked to reason about a dependency loop under pressure | deterministic check (loop described; SSH escape with the loopback address given) | 100% |
| **The runbook is proven, not asserted: kill the edge and recover** | On a maintenance window: stop the haproxy allocation, confirm every edge hostname is dead AND that direct LAN access to 4646 is also refused, then recover using only the runbook | Service restored using nothing but the runbook and an SSH session. **Confirming that 4646 is also refused during the outage is the point**: it proves the operator is genuinely in the trapped state the runbook describes, rather than quietly recovering through a hole the ticket was supposed to close | deterministic check (edge down, direct access also refused, recovery via runbook alone) | 100% |
| **The runbook's facts are pinned to the repo** | The runbook's tests | Every path, port, hostname and IP the runbook states is asserted against the repo file that declares it, following `D5-cli-breakglass`'s rule. A recovery document that drifts is worse than none, because it is consulted only when there is no time to check whether it is still true | deterministic check (each stated fact pinned to its source file) | 100% |
| **The runbook covers being off-LAN** | Read it | It states that after this change a developer on the tailnet but off the LAN reaches the edge on 443 and nothing else, and it names the SSH hop for anything more. `ufw` gives the tailnet port 22 and, via the haproxy entry, 80/443. Someone debugging from outside will otherwise conclude the cluster is down | deterministic check (tailnet reachability stated; SSH hop named) | 100% |
| **A fresh bootstrap still converges** | `just bootstrap` against a scratch or re-imaged node | Completes without a network error. Ansible configures services before haproxy exists, so anything it does over the network must be node-local or explicitly permitted. This ticket narrows the very ports bootstrap uses, and the breakage would only appear the next time a node is rebuilt, which could be months later | deterministic check (bootstrap converges on a fresh node) | 100% |
| **Guardrail: applied to a worker before the manager** | Read the ticket's close-out and the command history | A single worker was narrowed and verified before the manager was touched. The manager carries the Nomad server, Vault, the Consul server and haproxy; a mistake there takes the cluster and the recovery path at once. Doing all five in one run is the tempting shortcut this row exists to prevent | deterministic check (worker-first ordering evidenced) | 100% |
| **Guardrail: no default-allow is introduced** | `ufw status verbose` on every node | Default incoming policy is still `deny`, and no rule allows a service port from `any`. The role sets deny at `tasks/main.yml:7-13` and the whole exposure model rests on it | deterministic check (default deny intact; no `from any` service rule) | 100% |
| **Guardrail: intra-cluster ports are untouched** | Diff the firewall configuration for 4647, 4648, 8300, 8301, 8201, 8600 | Unchanged. These carry Nomad RPC and Serf, Consul gossip and Vault's cluster port. They are not services with an edge route, and narrowing them mid-ticket would break the cluster in a way that looks like this ticket worked | deterministic check (the six intra-cluster ports unchanged) | 100% |
| The repo gate passes | `just worktree_setup <path>`, then `just pre_commit` | All Passed. The change spans YAML, Terraform and markdown, so `check-yaml`, `terraform fmt`/`validate` and the doc slop scan all apply | deterministic check (`just pre_commit` all Passed) | 100% |

signed-off-by: PENDING
