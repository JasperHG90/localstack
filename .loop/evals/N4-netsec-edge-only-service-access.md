eval: N4-netsec-edge-only-service-access

**Definition of Done:** every service with an edge route is reachable only
through `https://<name>.lab.orangecluster.nl`. That set is exactly the ten
haproxy backends: nomad, vault, consul, minio, s3, phoenix, memex, grafana,
mlflow, bifrost (`deployments/infrastructure/services/haproxy.hcl:127-157`).
Direct connections to those service ports from a LAN host that is not a
cluster node are refused, the broad `192.168.0.0/16` rules are gone rather
than shadowed across **all three** provisioners, the in-cluster consumers that
cross node boundaries still reach their targets, this repo's own tooling talks
to the edge, every job still runs, and a runbook exists that gets an operator
out of every failure this change makes possible.

**The refusal row below scores that whole set**, not a subset. The previous
version of this marker scored seven URLs and its Definition of Done claimed
every edge-routed service, so a green marker was compatible with mlflow and
phoenix still open. Those two are now the rows the ticket is judged on.

**The trap that governs this whole marker.** No provisioner removes a firewall
rule. The Ansible role loops `ufw allow` and has no delete step
(`bootstrap/roles/firewall/tasks/main.yml:15-22`); both Terraform
`null_resource.firewall` blocks are create-only
(`deployments/infrastructure/services.tf:286-294`,
`deployments/applications/services.tf:93-101`), which is the defect `N3`
exists to fix for the Terraform side. So **narrowing a rule adds a narrow one
and leaves the broad one live**, `ufw` matches the broad one, and the port
stays open. A run reports `changed`, the config reads perfectly, and nothing
closed.

This is not hypothetical. `ufw status numbered` on radxa today shows
`[10] 8642/tcp ALLOW IN 192.168.0.0/16` surviving beside the two narrow rules
the repo declares (`deployments/applications/services.tf:46-47`). The failure
mode has already happened once, on the entry this ticket holds up as the good
pattern.

Therefore: **no closure in this marker may be scored by reading a config
file.** Every row asserting that something is now unreachable is scored from a
socket, attempted from a LAN host that is not a cluster node. The rows that do
read config are the guardrails, and they exist to catch the inverse error:
something reopened, or a rule keyed to a host that should not have one.

**The second trap: a test that cannot fail.** A `curl` that is refused proves
nothing on its own, because a typo in the hostname is also refused. Row 1
requires the **pre-change response to be recorded first**, so every closure is
a measured transition rather than an assertion about an unreachable address.

**Forks resolved 2026-07-31**, all four on their recommendations, and four
rows exist because of them. Q1 teaches the Ansible role to reconcile within
its own lane, so a row checks the role deletes an undeclared rule rather than
accepting a manual `ufw delete`, **and** that it leaves the Terraform-owned
rules on the same node standing. Q2 restricts 8404 to the Prometheus host, so
a row asserts it is still reachable from there. Q3 allows no devcontainer
exception, so a row bars any rule keyed to a non-cluster host. Q4 keeps the
explicit host ACLs, so a row pins the 503 on an unknown Host.

**Measured starting state, 2026-07-31.** All plaintext, all from the
devcontainer, all bypassing the edge: `192.168.2.30:4646` Nomad (307 to the
UI), `:8200` Vault 200, `:8500` Consul 200, `192.168.2.29:9001` MinIO console
200, `:9000` MinIO S3 200, **`192.168.2.50:5050` mlflow 200 with no
credentials while `https://mlflow.lab…` returns 401**, **`192.168.2.29:6006`
phoenix 200 with no credentials while `https://phoenix.lab…` returns 401**,
`192.168.2.46:8000` memex 401, `192.168.2.50:8080` bifrost 200,
`192.168.2.47:3000` grafana 302, `192.168.2.50:8642` hermes 404 (reachable,
no edge route), `192.168.2.30:8404` haproxy stats 200 unauthenticated, and
`firebat:4646` by mDNS name 307. `firebat.local:4646` does **not** resolve
from the devcontainer today and is therefore not scored.

**Tailnet starting state.** `100.64.0.0/10` reaches 80, 443, 8404
(`deployments/infrastructure/services.tf:191-192`), grafana 3000 (`:222`) and
the rpi4b's 8080 (`:231`). 8404 is not the only one.

| Behavior | Input | Expected | Scorer | Threshold |
|----------|-------|----------|--------|-----------|
| **The negative control is captured before the change** | Before touching any rule, from a non-cluster LAN host: request every URL in the measured-state paragraph, plus the edge equivalent of each, and record status and timing | All recorded, and the record is committed with the ticket. It must show the two 200/401 pairs (mlflow, phoenix) explicitly, because those pairs are the authentication bypass this ticket closes. Without this capture, every "refused" in the rows below is indistinguishable from a typo, an unplugged cable, or a host that was never listening. **This row is scored first and cannot be reconstructed afterwards** | deterministic check (every pre-change response recorded and committed, including both direct/edge pairs) | 100% |
| **Direct access is refused for EVERY edge-routed service** | After the change, from the same non-cluster LAN host: `192.168.2.30:4646`, `:8200`, `:8500`, `192.168.2.29:9001`, `:9000`, `192.168.2.29:6006` (phoenix), `192.168.2.46:8000` (memex), `192.168.2.47:3000` (grafana), `192.168.2.50:5050` (mlflow), `:8080` (bifrost) | Every one refused: connection refused, or timeout, or reset. **Not** a 401, 403 or 503, which would mean the packet still reached a listener and only the application declined. This ticket closes the network path, not the application. The ten addresses are exactly haproxy's ten backends, so this row and the Definition of Done cover the same set | deterministic check (all ten refused at the transport layer) | 100% |
| **The two authentication bypasses are closed (the priority row)** | `http://192.168.2.50:5050/` and `http://192.168.2.29:6006/` from a non-cluster LAN host, then `https://mlflow.lab.orangecluster.nl/` and `https://phoenix.lab.orangecluster.nl/` with no credentials | Both direct ports refused at the transport layer; both edge URLs still return **401**. haproxy gates both with `http-request auth unless { http_auth(openfang_users) }` (`haproxy.hcl:143` phoenix, `:153` mlflow) and the direct ports enforce nothing, so today an unauthenticated LAN client reads both. **This is the strongest single reason the ticket exists.** A marker that goes green while these two answer 200 has scored the wrong thing, which is exactly how the previous version of this marker failed | deterministic check (both direct ports refused; both edge URLs still 401) | 100% |
| **The broad rule is DELETED, not shadowed, across all three provisioners** | `ufw status numbered` on every node in `bootstrap/inventory/cluster.ini` | **No** `192.168.0.0/16` entry survives for 4646, 8200, 8500, `20000:32000` (Ansible), 9000, 9001, 3000, 8404, 8080-on-`.47` (infrastructure map), or 6006, 4317, 8000, 5050, 8080-on-`.50` (applications map). **8642 is on the list too**, even though this ticket changes no rule for it: the repo has declared hermes narrow since N1 (`applications/services.tf:46-47`) while radxa's live table still carries `[10] 8642/tcp ALLOW 192.168.0.0/16`, which is this plan's proof that shadowing is real. If 8642 still answers from a non-cluster LAN host after the run, N3 did not converge and every other closure here is suspect. A narrow rule added beside a surviving broad one is the single most likely failure of this ticket, it passes any config-file check, and `ufw` will match the broad rule. Enumerate the live table; do not infer it from the playbook | deterministic check (no broad entry for any closed port, 8642 included, on any node) | 100% |
| **The third provisioner was actually edited** | `deployments/applications/services.tf:22-102` and a `terraform plan` in `deployments/applications` | The `phoenix`, `memex`, `mlflow` and `bifrost` entries carry per-source rules in the shape of the existing `hermes` entry (`:41-48`), not `192.168.0.0/16`. This root is a **second** `null_resource.firewall` with its own map and SSH connections, and it owns mlflow 5050 and phoenix 6006, the two bypasses. A change that edits only `configure_network.yml` and the infrastructure map closes nothing that matters and still passes a casual read of the diff | deterministic check (all four entries narrowed in the applications map) | 100% |
| **Every cross-node consumer still reaches its target (positive control)** | From `192.168.2.46`: connect to `192.168.2.29:6006/v1/traces`, `192.168.2.50:8080/v1` and `192.168.2.29:9000`. From `192.168.2.50`: connect to `192.168.2.46:8000`, `192.168.2.29:9000`, `192.168.2.30:4646` and `:8500`. From `192.168.2.47`: connect to `192.168.2.30:8200`, `:4646`, `:8500`, `:8404` and `192.168.2.29:9000` | All twelve succeed, and they are exactly the plan's per-service target table. memex traces phoenix on a **different node** (`memex.hcl:143`), memex dials bifrost (`:148`, `:155`, `:158`) and minio (`:122`); hermes dials memex (`hermes.hcl:203`, `:449`), nomad and consul (`:205-206`, `:451-452`); mlflow (`mlflow.hcl:50`) and backup-minio (`backup-minio.hcl:37`, pinned to radxa by `:13-16`) dial minio from `.50`; loki dials minio from `.47` (`loki.hcl:119`); Prometheus scrapes from `.47` (`prometheus.hcl:85`, `:92`, `:96`, `:105`); and **acme on `.47` dials vault 8200 to renew the edge's own certificate** (`acme.hcl:163`, placed by `:17-20`), which its own comment says must not go through the proxy (`:158-161`). **Narrowing any of these to the edge alone is silent**: traces stop, memex's model calls and file store fail, the nightly backup stops, and the edge certificate expires about 60 days later. This row is the reason the plan carries a per-service target table instead of the word "edge", and the table was derived by sweeping the jobspecs rather than reading haproxy's backends | deterministic check (all twelve cross-node connections succeed) | 100% |
| **The mDNS name is closed too, not just the IP** | `http://firebat:4646/` from a non-cluster LAN host | Refused. `avahi-daemon` is installed on every node (`install_dependencies.yml`), so the hostname is a second route to the same socket, and the operator's own report named this form. A rule keyed on anything but the destination port and source address would close one and leave the other. **Only the bare `firebat` form is scored**: `firebat.local:4646` does not resolve from the devcontainer today, so requiring its refusal would pass before any change and measure nothing | deterministic check (`firebat:4646` refused) | 100% |
| **The Nomad dynamic port range is closed** | `ufw status numbered` on every node for `20000:32000`, plus a connection attempt to an arbitrary in-range port (say 24242) on firebat and radxa from a non-cluster LAN host, recorded before and after | No `20000:32000 ALLOW 192.168.0.0/16` entry survives on any node, and the in-range probe **transitions from an immediate RST (connection refused) to a timeout**. That direction is the whole signal: today the range is allowed with no listener, so the kernel answers with a reset; after the change ufw's default `deny` DROPs the packet (`bootstrap/roles/firewall/tasks/main.yml:7-13`) and the probe hangs. Reading it the other way round scores the pre-change state as a pass. **Scored this way deliberately.** Zero of the 24 running allocations use a port in that range today and `ss -ltn` shows no listener there, so "pick a live allocation's dynamic port and connect" has no subject and can neither pass nor fail | deterministic check (no broad range rule on any node; in-range probe goes from RST to timeout) | 100% |
| **The tailnet keeps 80, 443 and SSH-to-firebat, and nothing else** | From a tailnet address: `http://192.168.2.30:8404/`, `http://192.168.2.47:3000/`, `http://192.168.2.47:8080/`, and `https://192.168.2.30/` | The first three not reachable; 443 still serves. Today `100.64.0.0/10` reaches 8404 (`deployments/infrastructure/services.tf:191-192`), grafana 3000 (`:222`) and the rpi4b's 8080 (`:231`). The previous version of this marker asserted 8404 was the only one, which is false and would have left two tailnet holes open behind a green row. Q2 additionally restricts 8404 to the Prometheus host | deterministic check (8404, 3000 and 8080 unreachable from tailnet; 443 still serves) | 100% |
| **8404 is still reachable FROM the Prometheus host (positive control)** | Connect to `192.168.2.30:8404/metrics` from `192.168.2.47`, the Prometheus host | 200 with metrics. Q2 kept scraping as 8404's real job: `haproxy.hcl:122` serves `/metrics` via `http-request use-service prometheus-exporter`, and `prometheus.hcl:96` scrapes it. **Closing 8404 to everything passes the row above and is a failure**, and its only symptom is an empty Grafana panel discovered days later. Every closure row in this marker needs its matching positive control, and this is 8404's | deterministic check (200 from the Prometheus host with metrics) | 100% |
| **The Ansible role RECONCILES, and its prune stays in its own lane** | Add a throwaway rule via the role, remove it from `firewall_ufw_ports`, re-run the playbook, then read `ufw status numbered` on that node | The throwaway rule is **gone from the host**, and **every Terraform-owned rule on that node is still present** (on `.30`: minio nothing, haproxy 80/443, the registry, node exporter 9100, postgres exporter 9187). Q1 resolved to teach the role to delete what it no longer declares, scoped to the ports the role declares. Both halves are scored because they fail in opposite directions: a role that prunes nothing leaves the broad rules live, and a role that prunes everything not in `firewall_ufw_ports` wipes the Terraform half on the next `just bootstrap`. N3 carries the mirror constraint on its side. **This row also exists because the broad-rule row above passes against a one-off `ufw delete`**, which fixes today's rules and leaves the accumulate-only role intact so the next narrowing hits the same bug | deterministic check (undeclared rule removed by a normal run; Terraform-owned rules on the same node untouched) | 100% |
| **Guardrail: no rule is keyed to a non-cluster host** | Read the final rule set in `configure_network.yml`, the infrastructure firewall map and the applications firewall map | Every `from_ip` is a cluster node address, `192.168.0.0/16` for port 22 and the edge ports, or `100.64.0.0/10` for port 22 on firebat and for 80/443. **No entry pins a developer machine, a laptop, or the devcontainer.** Q3 resolved there is no exception: such an allowance depends on a DHCP address that will change, and it silently reopens exactly what this ticket closes for whoever inherits that lease. All three provisioners are read, not two | deterministic check (no non-cluster host address in any of the three rule sets) | 100% |
| **Guardrail: haproxy still refuses an unknown Host** | `curl -k -H 'Host: bogus.example.com' https://192.168.2.30/` | 503, as measured 2026-07-31. The guarantee is a 443 guarantee: port 80 answers 301 to https for an unknown Host and only the redirect target 503s. Q4 resolved to keep the ten explicit `hdr(host)` ACLs with no wildcard and no `default_backend`. A wildcard `hdr(host) -m end .lab.orangecluster.nl` plus a default backend would route unknown names somewhere instead of refusing them, inverting the point of the ticket. The list is the allowlist, and this row stops a later convenience edit from quietly removing it | deterministic check (unknown Host returns 503 on 443; no wildcard ACL; no default_backend) | 100% |
| **Postgres, the registry, loki, NATS and the exporters are knowingly untouched** | `ufw status` for 5432, 5000/5001, 3100, 4222/8222 and 9100/9187 | Unchanged and explicitly recorded as out of scope in the close-out. None has an edge route, so "use the hostname" is meaningless for them, and loki, Prometheus and the exporters are already narrow. hermes 8642 is **not** on this list: it is in scope as cleanup of the stale broad rule, scored in the broad-rule row. This row exists so the boundary is a decision on the record rather than an oversight, and so nobody silently firewalls a database port inside a networking ticket | deterministic check (those ports unchanged; exclusion recorded) | 100% |
| **All ten edge hostnames still serve** | `https://<h>.lab.orangecluster.nl` for minio, s3, vault, nomad, consul, phoenix, memex, grafana, mlflow, bifrost | Every one answers as it did before the change, compared against the pre-change capture from row 1. mlflow and phoenix answer 401 without credentials and their content with them; a 503 on either means haproxy lost its backend to the narrowing. The edge is the only remaining way in, so a hostname that stops working is now a total outage for that service rather than an inconvenience | deterministic check (all ten match their pre-change responses) | 100% |
| **The full API still proxies, not just the UI** | `https://nomad.lab.orangecluster.nl/v1/agent/health`, `https://vault.lab.orangecluster.nl/v1/sys/health`, `https://consul.lab.orangecluster.nl/v1/status/leader`, `https://s3.lab.orangecluster.nl/minio/health/live` | All 200. Verified 200 before the change. Terraform and the CLI drive the API, not the UI, so a change that preserves the web interface while breaking `/v1/` would pass a browser check and break every deploy | deterministic check (all four API paths 200 through the edge) | 100% |
| **The cluster is unchanged underneath** | `nomad node status`, `nomad job status`, `vault status`, `consul members`, and an allocation census | 5 nodes ready, 19 jobs running, 24 allocations with none pending or failed, Vault unsealed on Consul storage, Consul quorum intact. These are the pre-change numbers. Intra-cluster traffic uses ports this ticket does not touch, so a regression here means it closed something it did not intend to | deterministic check (node, job, alloc, seal and quorum state all match pre-change) | 100% |
| **Prometheus is still scraping** | Query Prometheus for scrape health across its targets | No target that was up before is down after. Prometheus on `192.168.2.47` scrapes nomad 4646, consul 8500, haproxy 8404, minio 9000, bifrost 8080, postgres 9187 and every node exporter (`prometheus.hcl:85-135`), so it is the most sensitive consumer of narrowed rules and its failure is silent until a dashboard is empty days later | deterministic check (no target transitions up to down) | 100% |
| **Both Terraform roots init AND plan clean from the devcontainer** | `just init` then a plan in `deployments/infrastructure` and `deployments/applications`, using the migrated addresses | Both exit 0 with no error. **`init` is scored separately from `plan` because it fails one step earlier**: the Consul state backend is `address = "192.168.2.30:8500"`, `scheme = "http"` in both `vars/backend-config.hcl:1-3`, so a narrowed 8500 breaks `init` before a provider is ever reached. The plan half then exercises the Vault, Nomad, Consul **and MinIO** providers over the edge in one go: the MinIO provider dials `<consul node address>:9000` (`applications/providers.tf:45`) and `minio_iam_user`/`minio_accesskey` resources exist (`applications/storage.tf:46`, `:51`), so a plan touches 9000 on every run. Key presence is not proof; a clean init and plan is | deterministic check (both roots init and plan, exit 0) | 100% |
| **No raw service address survives in the repo's own client config** | `grep -rnE '192\.168\.2\.(29\|30\|46\|47\|50):(4646\|8200\|8500\|9000\|9001)' .devcontainer/ deployments/*/providers.tf deployments/*/vars/` | No match. The scope is deliberately **the addresses this repo dials from outside the cluster**, not every literal IP in the tree. `.devcontainer/.env` carries four (`:3`, `:6`, `:9`, `:18`), `deployments/applications/providers.tf:40` hardcodes `192.168.2.30:8500`, `:45` builds the MinIO address from Consul, and both `vars/backend-config.hcl:1` point the Terraform state backend at plaintext 8500; all of them must move. **Everything else stays and must not be swept up:** haproxy's backends (`haproxy.hcl:127-157`), the two firewall maps, and in-cluster job-to-job addresses, which are node-to-node traffic and are load-bearing whether they are template variables (`memex.hcl:122`) or literals (`hermes.hcl:205-206`, `acme.hcl:163`, `prometheus.hcl:85`, `loki.hcl:119`). A repo-wide grep here would be red no matter what the implementer does, and a row that cannot pass gets waived | deterministic check (no raw service address under `.devcontainer/`, `providers.tf`, or `vars/`) | 100% |
| **`.devcontainer/.env.example` teaches the new world** | Read it | `NOMAD_ADDR`, `CONSUL_HTTP_ADDR`, `VAULT_ADDR` and `MINIO_ENDPOINT` are HTTPS edge hostnames. Today they are **mDNS names, not IPs**: `http://localstack.local:4646` (`:3`), `http://localstack.local:8500` (`:7`), `http://localstack.local:8200` (`:11`), `http://orangepirv2.local:9000` (`:23`). Those names resolve to the same sockets this ticket closes, so the template is what the next person copies and it hands them a config that cannot work and looks official | deterministic check (all four are HTTPS edge hostnames) | 100% |
| **SSH is untouched, and the recovery path is what actually exists** | `ufw status` on all five nodes, an SSH connection to firebat from LAN and from tailnet, and an SSH connection to one worker from LAN | Port 22 on **firebat** still allowed from `192.168.0.0/16` **and** `100.64.0.0/10` (`configure_network.yml:10-11`), both connections succeeding. Port 22 on the **four workers** still allowed from `192.168.0.0/16` only (`:35`), LAN connection succeeding. **No tailnet assertion is made for the workers**: `configure_tailscale.yml:3` targets `manager`, so only firebat has tailscale at all, and `tailscale ip -4` on the rpi4b returns nothing. The previous version of this row required tailnet SSH on all five nodes, which does not exist and cannot be reached without scoping a tailscale rollout into this ticket | deterministic check (firebat 22 from both ranges; workers 22 from LAN only; no worker tailnet rule added) | 100% |
| **The recovery runbook exists and every command in it works** | Run each command in `docs/edge-and-recovery.md` against the live cluster | Every command executes and does what the runbook says. A runbook is worth nothing if it was written from memory: the failure it covers is exactly when nobody has the patience to debug a typo in the fix | model + rubric (adversarial review agent) | 5/5 |
| **The runbook answers BOTH circular dependencies by name** | Read `docs/edge-and-recovery.md` | First: haproxy is a Nomad job, after this change the only route to Nomad's API is haproxy, so a dead haproxy cannot be restarted through the edge. Escape given: SSH to firebat, then `NOMAD_ADDR=http://127.0.0.1:4646 nomad job restart haproxy`. Second: haproxy's TLS PEM is rendered from Vault (`haproxy.hcl:39`, `:62-72`) and `just unseal_vault` drives `scripts/unseal_vault.sh` against `$VAULT_ADDR` from the devcontainer, so **a sealed Vault plus a haproxy restart leaves no edge to unseal through**. Escape given for that too. **These are the failures this ticket creates**, and an operator who has to derive either one during an outage is being asked to reason about a dependency loop under pressure | deterministic check (both loops described; SSH escape with the loopback address given for each) | 100% |
| **The runbook is proven, not asserted: kill the edge and recover** | On a maintenance window: stop the haproxy allocation, confirm every edge hostname is dead AND that direct LAN access to 4646 is also refused, then recover using only the runbook | Service restored using nothing but the runbook and an SSH session. **Confirming that 4646 is also refused during the outage is the point**: it proves the operator is genuinely in the trapped state the runbook describes, rather than quietly recovering through a hole the ticket was supposed to close | deterministic check (edge down, direct access also refused, recovery via runbook alone) | 100% |
| **The runbook's facts are pinned to the repo** | The runbook's tests | Every path, port, hostname and IP the runbook states is asserted against the repo file that declares it, following `D5-cli-breakglass`'s rule. A recovery document that drifts is worse than none, because it is consulted only when there is no time to check whether it is still true | deterministic check (each stated fact pinned to its source file) | 100% |
| **The runbook covers being off-LAN, including the worker gap** | Read it | It states that after this change a developer on the tailnet but off the LAN reaches the edge on 443 and reaches SSH on **firebat only**, and that a worker is reachable only by hopping through firebat, because tailscale is installed on the manager alone (`configure_tailscale.yml:3`). Someone debugging from outside will otherwise conclude the cluster is down, or worse, assume a worker is reachable and discover otherwise mid-incident | deterministic check (tailnet reachability stated; firebat-only SSH and the worker hop named) | 100% |
| **The openfang credential exposure is recorded and handed to its own ticket** | Read the plan's non-goals and the ticket ledger | A separate ticket exists to render the openfang basic-auth password from Vault through a `template` stanza, and this plan names it as a non-goal. The password is interpolated into the haproxy jobspec (`deployments/infrastructure/services.tf:322`) and the running job carries it as a **literal** (`haproxy.hcl:89`), readable by anyone with Nomad `read-job`, which both the `deploy` and `developer` policies grant. **Closing the firewall does not fix this**: it is the credential protecting the two services this ticket is mainly about. This row exists so the finding is tracked rather than absorbed and forgotten inside a networking ticket | deterministic check (follow-up ticket exists and is referenced from the non-goals) | 100% |
| **A fresh bootstrap still converges** | `just bootstrap` against a scratch or re-imaged node | Completes without a network error. Ansible configures services before haproxy exists, so anything it does over the network must be node-local or explicitly permitted. This ticket narrows the very ports bootstrap uses, and the breakage would only appear the next time a node is rebuilt, which could be months later | deterministic check (bootstrap converges on a fresh node) | 100% |
| **Guardrail: applied to a worker before the manager** | Read the ticket's close-out and the command history | A single worker was narrowed and verified before the manager was touched. The manager carries the Nomad server, Vault, the Consul server and haproxy; a mistake there takes the cluster and the recovery path at once, and firebat is the only node with a tailnet fallback. Doing all five in one run is the tempting shortcut this row exists to prevent | deterministic check (worker-first ordering evidenced) | 100% |
| **Guardrail: no default-allow is introduced** | `ufw status verbose` on every node | Default incoming policy is still `deny`, and no rule allows a service port from `any`. The role sets deny at `tasks/main.yml:7-13` and the whole exposure model rests on it | deterministic check (default deny intact; no `from any` service rule) | 100% |
| **Guardrail: intra-cluster ports are untouched** | Diff all three firewall configurations for 4647, 4648, 8300, 8301, 8201, 8600 | Unchanged. These carry Nomad RPC and Serf, Consul gossip and Vault's cluster port. They are not services with an edge route, and narrowing them mid-ticket would break the cluster in a way that looks like this ticket worked | deterministic check (the six intra-cluster ports unchanged) | 100% |
| The repo gate passes | `just worktree_setup <path>`, then `just pre_commit` | All Passed. The change spans YAML, Terraform and markdown, so `check-yaml`, `terraform fmt`/`validate` and the doc slop scan all apply | deterministic check (`just pre_commit` all Passed) | 100% |

signed-off-by: PENDING
