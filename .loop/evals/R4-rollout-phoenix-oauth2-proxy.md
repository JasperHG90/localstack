eval: R4-rollout-phoenix-oauth2-proxy

Definition of Done: browsing the Phoenix console requires a completed Vault
OIDC login through Phoenix's own generic OIDC client, with no oauth2-proxy in
front of it, while OTLP trace ingest keeps working because `memex` now presents
a bearer key, ingest without a key is refused, and the old shared HAProxy basic
auth on the Phoenix backend is gone with no double auth.

Rewritten 2026-07-26 alongside the plan. The previous marker's Definition of
Done encoded the oauth2-proxy design and its port-6006 split, which the plan
rewrite removes. **Note the reversal in the ingest rows:** ingest passing used
to mean `/v1/traces` answering *without* auth. It now means answering *with* a
bearer key and refusing one *without*.

Rows re-cut 2026-08-04 after the plan-validator found two that fail a correct
implementation. The old UI-gate rows expected `GET /` to return a `302` and
then followed the chain with `curl -L`. Phoenix is a single-page app: `GET /`
returns `200` with the HTML shell whether or not auth is on, and its redirect
to Vault is client-side, which `curl -L` cannot follow. Both rows are replaced
by server-side probes, and a guardrail row now asserts that the `200` is
correct, so nobody satisfies the marker by reintroducing a proxy.

Preconditions (plan §4, §8): **F2** and **T3** are both `done`. F2 shipped the
`lab` OIDC provider but only a `groups` scope, so this ticket adds the `email`
scope, its entry in `scopes_supported`, and the entity metadata behind it. Run
each row after `terraform apply` and the Nomad deploy have landed.

The ingest rows are the load-bearing guardrails: a pass requires the UI to be
gated **and** ingest to keep working. Breaking either side is a fail. Enabling
Phoenix auth stops trace collection silently, so an unexercised ingest row is
not evidence of anything.

| Behavior | Input | Expected | Scorer | Threshold |
|----------|-------|----------|--------|-----------|
| Phoenix is running and healthy after apply, with no proxy job added | `nomad job status phoenix`; `nomad job status` | the `phoenix` task reports `running`/`healthy` and deployment `Status = successful`; no `phoenix-oauth2-proxy` (or equivalent) job exists, since this design adds no proxy | deterministic check (`nomad job status phoenix`) | 100% |
| The API surface refuses an unauthenticated caller, which is where Phoenix's gate actually lives | `curl -si -o /dev/null -w '%{http_code}\n' -X POST http://192.168.2.29:6006/graphql -H 'content-type: application/json' -d '{"query":"{ __typename }"}'`, and separately confirm the response carries no `www-authenticate: Basic` header | `401` or `403` **from Phoenix**. Probe the **node** address, not the edge: HAProxy's shared basic auth answers `https://phoenix.lab.orangecluster.nl/graphql` with `401` and `www-authenticate: Basic realm="phoenix"` today (measured 2026-08-04), while the node itself returns `200` — so an edge probe passes this row before the ticket has done anything, and keeps passing if Phoenix's own gate is never enabled. Phoenix is a single-page app: with auth on, `GET /` still returns `200` with the HTML shell and the gate lives on the API/GraphQL layer, so probing `/` for a redirect tests the wrong thing | deterministic check (`curl` to the node's API, expecting 401/403 with no Basic challenge) | 100% |
| The OIDC login path reaches Vault, proving the client is wired and not just that some login exists | `curl -s -D - -o /dev/null --request GET 'https://phoenix.lab.orangecluster.nl/oauth2/vault/login'` and read the `Location` header. **Use GET, never `curl -sI`** | a `302` whose `Location` is `https://vault.lab.orangecluster.nl/ui/vault/identity/oidc/provider/lab/authorize`, carrying the Phoenix `client_id`. Note the `/ui/` path and the `lab` provider name: that is what the live discovery document advertises, and asserting the provider explicitly stops the probe passing against Vault's built-in `default` provider, whose `allowed_client_ids` is `["*"]`. **`curl -sI` sends HEAD and cannot work here.** FastAPI does not add HEAD to a GET route, so Phoenix's SPA catch-all answers every HEAD with `200 text/html` — measured 2026-08-04: `HEAD /arize_phoenix_version` and `HEAD /definitely-not-a-route` both return `200 text/html`, while `GET /arize_phoenix_version` returns `200 text/plain`. A HEAD probe here fails a correct implementation and cannot distinguish a wired route from a nonexistent one | deterministic check (`curl` GET on the login route, reading `Location`) | 100% |
| Guardrail: `GET /` returning 200 SPA HTML is CORRECT and must not be "fixed" | `curl -s -o /dev/null -w '%{http_code}\n' https://phoenix.lab.orangecluster.nl/` (GET, not HEAD — every HEAD returns 200 text/html from the SPA catch-all and proves nothing) | `200` with the Phoenix HTML shell is the expected, passing result. This row exists because the previous marker demanded a `302` here, which no correct native-auth implementation produces, and chasing it pushes an implementer back toward putting a proxy in front of Phoenix — the design this ticket removes | deterministic check (`curl` GET, expecting 200) | 100% |
| The issued ID token actually carries an `email` claim | Obtain a token one of two ways, both runnable by the operator: (a) complete an authorization-code flow against a client whose redirect URI the operator controls, requesting `scope=openid email`, then `base64 -d` the id_token's payload segment; or (b) call the `lab` provider's `userinfo_endpoint` (`https://vault.lab.orangecluster.nl/v1/identity/oidc/provider/lab/userinfo`) with a token from that flow — it applies the same scope templates | a non-empty `email` claim is present. Vault drops a scope it does not advertise without erroring, so a token can be signed, valid, and claim-less while the login appears to succeed; Phoenix then fails the callback with `missing_email_scope`. This row is what proves the scope template landed | deterministic check (decode the id_token payload, or read `userinfo`) | 100% |
| OTLP ingest WITH the bearer key is accepted, so `memex` traces keep arriving (load-bearing guardrail) | `curl -s -o /dev/null -w '%{http_code}\n' -X POST http://192.168.2.29:6006/v1/traces -H 'content-type: application/json' -H "authorization: Bearer $PHOENIX_KEY" -d '{"resourceSpans":[]}'` | a non-auth status from Phoenix's collector itself (`200`, `415`, or a `400` parse error), proving the request was authenticated and reached the collector; NOT `401`/`403`. **Probe the node directly, not the edge.** `memex` sends to `http://<phoenix_host>:6006/v1/traces` (`memex.hcl:169`), so the node address is the faithful path — and until the HAProxy basic-auth line at `haproxy.hcl:143` is removed, an edge probe can be answered by HAProxy rather than Phoenix, which would make this row test the wrong component | deterministic check (`curl -X POST` to the node, with the key) | 100% |
| OTLP ingest WITHOUT a key is refused, proving ingest is genuinely gated and not merely still open | same POST as the row above, to the same node address, with the `authorization` header omitted | `401` or `403` **from Phoenix**, with no `WWW-Authenticate: Basic` in the response — that header would mean HAProxy's shared basic auth answered instead, which proves nothing about whether Phoenix gates its own collector. A `200` means auth is not enforced on the collector and the ticket has not achieved its second goal | deterministic check (`curl -X POST` to the node, no key, inspecting headers) | 100% |
| A real trace from `memex` lands in Phoenix end to end, not just a synthetic POST | exercise a `memex` operation that emits spans, then query Phoenix for recent traces for `service.name = memex` (UI or API) | at least one new `memex` span appears, timestamped after the apply; an empty result means ingest is dark even if the synthetic POST passed | deterministic check (trace query after a known operation) | 100% |
| The old shared basic auth no longer fronts Phoenix, and is not stacked on top of Phoenix's own login | `curl -s -D - -o /dev/null https://phoenix.lab.orangecluster.nl/` and inspect the response headers; and inspect the live `backend phoenix` block in the running HAProxy config | no `WWW-Authenticate: Basic` on the Phoenix route and no `http-request auth ... http_auth(openfang_users)` in the `phoenix` backend; MLflow's line (`haproxy.hcl:153`) left intact. Bifrost is NOT checked: B1 removed its basic-auth line in `ac3267b`, so `backend bifrost` has none to preserve | deterministic check (`curl` GET with `-D -` + config inspection) | 100% |
| Behind a completed Vault login the console renders, and the auth-versus-ingest split holds under review | complete the OIDC flow in a browser and load the console; hand the reviewer the ingest evidence from the rows above per `.claude/rules/adversarial-reviews.md` | the console renders authenticated with no broken assets or redirect loop, AND the reviewer confirms ingest works with a key and is refused without one | model + rubric (adversarial review agent) | 4/5 |
