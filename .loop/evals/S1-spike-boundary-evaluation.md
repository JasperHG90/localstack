eval: S1-spike-boundary-evaluation

Definition of Done: `docs/rfcs/boundary-evaluation.md` exists and answers the
spike — an evidence-backed KEEP/DROP verdict, a two-layer deploy footprint, the
Vault engine inventory against the live state, and a good-fit/poor-fit split —
with no Zitadel reference and no infrastructure shipped.

The path is `docs/rfcs/`, per the operator's resolved fork of 2026-07-23. The
directory does not exist yet and the ticket creates it. No row names
`docs/notes/`.

| Behavior | Input | Expected | Scorer | Threshold |
| --- | --- | --- | --- | --- |
| The decision doc exists at the agreed path | `test -f docs/rfcs/boundary-evaluation.md` | Exit code 0 — the file is present at `docs/rfcs/boundary-evaluation.md` | deterministic check (`test -f docs/rfcs/boundary-evaluation.md`) | 100% |
| The reader gets an unambiguous, evidence-backed verdict | Read the verdict section of `docs/rfcs/boundary-evaluation.md` | Exactly one of KEEP or DROP stated as a verdict line, justified by solo-operator deploy/maintenance cost vs benefit; the verdict does NOT rest solely on "Terraform can express the config" | model + rubric (adversarial review agent) | 4/5 |
| The doc enumerates the footprint across both deploy layers | `grep -q -e boundary_controller -e boundary_worker docs/rfcs/boundary-evaluation.md && grep -q 'hashicorp/boundary' docs/rfcs/boundary-evaluation.md` | Exit 0 — bootstrap layer names a `boundary_controller` (or `boundary_worker`) role AND the Terraform layer names the `hashicorp/boundary` provider. Alternation is expressed with two `-e` patterns rather than a regex pipe, because a `\|` written to survive this markdown table reaches `grep -E` as a literal escaped pipe and matches nothing a correct doc contains | deterministic check (`grep -q -e boundary_controller -e boundary_worker … && grep -q 'hashicorp/boundary' …`) | 100% |
| The Vault engine inventory matches the live state | `grep -q 'S2' docs/rfcs/boundary-evaluation.md && grep -q 'database/creds/s2-poc' docs/rfcs/boundary-evaluation.md && grep -q 'transit/' docs/rfcs/boundary-evaluation.md && grep -q 'secrets.tf' docs/rfcs/boundary-evaluation.md` | Exit 0 — the doc names the `database` engine S2 left running (`database/creds/s2-poc`) as the credential library a PoC would broker, ties it to ticket S2, names the `transit/` mount as the engine the footprint still adds for Boundary's KMS, and cites `secrets.tf` for the KV2 mount **as one engine among several**. The trailing slash on `transit/` matters: bare `transit` matches the phrase "encrypted in transit", which a doc discussing HAProxy TLS will almost certainly contain, so the looser pattern passes a doc that never mentions the Vault engine. Note the reversal from the previous marker, which required the doc to call `secrets.tf:2-7` "the sole engine that exists today": that is now false, and a doc repeating it fails the rubric rows even while passing this one | deterministic check (`grep -q 'S2' … && grep -q 'database/creds/s2-poc' … && grep -q 'transit/' … && grep -q 'secrets.tf' …`) | 100% |
| The reader can tell good-fit from poor-fit and the verdict is closed out | Read the fit-analysis and closing sections of `docs/rfcs/boundary-evaluation.md` | Wire-level/SSH good-fit targets (Postgres, MinIO S3 API, NATS, Phoenix gRPC, Memex API, node SSH) are separated from the web-UI poor-fit set (Grafana, MinIO console, MLflow, Phoenix UI); on KEEP the doc states minimal PoC scope, on DROP it states reopen conditions | model + rubric (adversarial review agent) | 4/5 |
| The verdict rests on evidence the plan did not already supply | Read the verdict and its supporting sections, then diff the claims against `.loop/plans/S1-spike-boundary-evaluation.md` | At least one load-bearing input to the verdict was weighed here for the first time — the Tailscale-SSH overlap (`bootstrap/roles/tailscale/`, Q5), the live `nomad/`/`consul/`/`database/` dynamic-credential precedent, or the measured cost of adding a Transit mount. A doc that only restates the plan's Context and Non-goals produces no finding and fails | model + rubric (adversarial review agent) | 4/5 |
| Guardrail: the dropped Zitadel design is never referenced | `! grep -qi zitadel docs/rfcs/boundary-evaluation.md` | Exit 0 — no case-insensitive match for `zitadel` anywhere in the doc. Negated `grep -q`, not `grep -c`: a count scorer exits 1 on zero matches and would mark the clean case a failure | deterministic check (`! grep -qi zitadel docs/rfcs/boundary-evaluation.md`) | 100% |
| Guardrail: no infrastructure ships — the change is doc-only | `git status --porcelain -- bootstrap deployments` AND `git diff --name-only HEAD -- bootstrap deployments`, both empty, and `test -f docs/rfcs/boundary-evaluation.md` exits 0 | Both commands print nothing and the file exists: the deliverable is there and no file under `bootstrap/` or `deployments/` was added, changed, **or left untracked**. `git status --porcelain` is not redundant — `git diff HEAD` never reports untracked files, so a new `deployments/infrastructure/boundary.tf` or a whole `bootstrap/roles/boundary_controller/` left unstaged passes the diff check outright. The eval is scored before the commit, so untracked IS the live state at scoring time, and untracked scaffolding is exactly the shape scope-creep-into-a-build takes. Scoped to the two product directories on purpose: an unscoped check also lists the `.loop/` artifacts the loop commits alongside the work, so demanding one path alone can never pass | deterministic check (both git commands empty for `bootstrap`/`deployments`, AND `test -f` on the deliverable) | 100% |
