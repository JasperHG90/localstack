eval: S2-spike-postgres-vault-creds

Definition of Done: the spike REACHES a decision on Path A (Vault database
secrets engine) with live evidence from firebat PG18 — a short-lived Postgres
user is minted, read by a Nomad Workload-Identity job, and performs a
privileged operation against a real app-owned object — the rotation-in-pools
wrinkle is characterized against a role with a short `max_ttl`, the Path B
validator-library question is actually investigated rather than asserted, and
the doc records whichever conclusion the evidence supports. All without
pointing the engine at the real root creds.

*Rewritten 2026-07-25. The previous marker asserted "Path A is proven" as the
Definition of Done and handed the scorer the Path B rejection rationale as the
expected answer, so no observation could have produced a "no". Row 4 tested
`select 1`, which needs only CONNECT and passes for a role with zero object
privileges — leaving R3's stated failure mode 4 untested. Row 5 rewarded
editing the shared Ansible policy, which F3 superseded.*

| Behavior | Input | Expected | Scorer | Threshold |
| --- | --- | --- | --- | --- |
| The Vault database engine is mounted and its connection points at firebat PG18 | `vault read database/config/<poc-conn>` | Config prints with `plugin_name` = `postgresql-database-plugin` and an `allowed_roles` list containing the PoC role; no error | deterministic check (`vault read database/config/<poc-conn>`) | 100% |
| The DB connection uses a dedicated PoC admin, never the real root creds, with root rotation left off | `vault read database/config/<poc-conn>` and inspect the configured admin identity | Connection `username` is the dedicated `vault-dbengine-admin` role, NOT the real `localstack` root; Vault-managed root rotation is not enabled. **This is the outage guard** — rotating the root would break the postgres_exporter, `backup-postgres`'s `pg_dumpall`, and the applications-layer provider, all of which authenticate as `localstack` | deterministic check (`vault read database/config/<poc-conn>`) | 100% |
| A PoC role mints a fresh short-lived Postgres user on demand, bounded by `max_ttl` | `vault read database/creds/<poc-role>`; then `vault read database/roles/<poc-role>` | A fresh `username`/`password` pair with a non-zero `lease_id` and short `lease_duration`; **and the role has an explicit short `max_ttl`**, not just `default_ttl`. Without `max_ttl` the lease renews to Vault's 768h default and the rotation test below cannot fire | deterministic check (`vault read database/roles/<poc-role>` shows a short `max_ttl`) | 100% |
| **The minted user can do the app's work, not merely connect** — the ticket's hardest unknown | Using the minted credential against a real app database (memex or ducklake): perform a privileged operation on an object owned by the existing static role — e.g. `INSERT`/`SELECT` on an existing table, or the operation a migration would run. `psql` is absent from this environment; use `docker run --rm postgres:18 psql ...` or run from inside the PoC alloc | The operation SUCCEEDS, demonstrating that `creation_statements` reproduced the grants and ownership the app needs. A bare `select 1` does NOT satisfy this row — it requires only CONNECT and passes for a role with no object privileges. **Failure here is a legitimate spike outcome and must be recorded as such, not worked around**: it is the falsifier for Path A and would mean R3 should not proceed | deterministic check (privileged operation succeeds against a real owned object) | 100% |
| The PoC job reads `database/creds/<role>` through a DEDICATED JWT role, leaving the shared policy untouched | Inspect `deployments/infrastructure/database-secrets-poc.tf` and the PoC jobspec; run the job and `nomad job status <poc-job>`; then `git diff bootstrap/` | A `vault_policy` scoped to `database/creds/<role>` plus a `vault_jwt_auth_backend_role` bound to the PoC job exist (pattern `pki.tf:69-116`), the job selects it with `vault { role = ... }`, the alloc reaches `running` and connects. **`git diff bootstrap/` is empty** — `vault_nomad_workloads.hcl.j2` is NOT edited, so no other workload gains `database/creds/*` | deterministic check (`nomad job status` running AND `git diff bootstrap/` empty) | 100% |
| The one-token trade-off is recorded for R3 | Review the decision doc | The doc states that a Nomad task holds exactly one Vault token, so a dedicated role REPLACES `nomad-workloads` — meaning any converted job (memex's `db-migrate` prestart, mlflow, phoenix) needs both the DB grant and its KV grants on the same role — and names which option it recommends R3 take | model + rubric (adversarial review agent) | 4/5 |
| The rotation-in-pools behavior is characterized against a lease that actually expires | Hold an idle pooled connection past `max_ttl`, let Vault revoke, then check out from the pool | The doc records the observed behavior for existing and new checkouts after revocation, with at least one mitigation. An inconclusive result is acceptable ONLY if the doc shows `max_ttl` was short enough for the lease to have expired during the window | model + rubric (adversarial review agent) | 4/5 |
| Path B was investigated, not assumed | Review the decision doc's Path B section | The doc names the PG18 OAuth validator module it evaluated and states whether one exists that accepts Vault/Nomad-issued JWTs, with a citation. "Rejected on validator-library availability" with no evidence of having checked is a FAIL | model + rubric (adversarial review agent) | 4/5 |
| The doc reaches a decision the evidence supports, and states its falsifier | Review the decision doc end to end | The doc states up front what observation would have ruled Path A out, reports what was actually observed, and concludes accordingly. A recommendation that does not follow from the recorded evidence — in either direction — is a FAIL | model + rubric (adversarial review agent) | 4/5 |
