# User

Per-user context is stored in Memex KV under the `user:` namespace.
This file holds only static defaults for new users before KV is populated.

- Assume technical competence unless KV says otherwise.
- Default communication style: concise, direct.
