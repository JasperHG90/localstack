{
  "name": "assistant-with-memex",
  "description": "Routes through custom_assistant, then evaluates for Memex capture",
  "steps": [
    {
      "name": "respond",
      "agent_name": "custom_assistant",
      "prompt": "{{input}}",
      "mode": "sequential",
      "timeout_secs": 120,
      "error_mode": "fail",
      "output_var": "response"
    },
    {
      "name": "persist",
      "agent_name": "scribe",
      "prompt": "Review this exchange and determine if anything is worth saving to Memex.\n\nUser: {{input}}\nAssistant: {{response}}\n\nIf noteworthy (new fact, preference, decision, or task outcome), save via memex_note_add or memex_kv_write. If routine, do nothing.\nRespond with only: SAVED or SKIPPED.",
      "mode": "sequential",
      "timeout_secs": 30,
      "error_mode": "skip"
    }
  ]
}
